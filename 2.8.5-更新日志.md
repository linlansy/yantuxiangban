# 研途相伴 2.8.5 更新日志

## 宠物动作修复

- 星星睡衣已接入原本遗漏的看书逐帧动画。
- 图书馆马甲与星空限定礼服现已支持挥手、看书、伸懒腰、庆祝、喝水、趴下、点击互动和喂食动作。
- 加大两套服装的动作幅度，并为看书、喝水、庆祝等动作增加对应道具或特效，避免只看到轻微晃动。
- 五种外观继续共用喂食与温柔拒绝动作，服装不会在播放动作时消失。

## AI 食物识别修复

- “测试连接”不再访问兼容性不稳定的模型列表接口，改为真实调用当前配置的 `chat/completions`。
- 自动兼容百炼根地址和完整兼容模式地址，避免重复或漏拼接口路径。
- 连接失败会区分 API Key/模型权限、接口地址/模型名、额度/限流和普通服务错误。
- 食物识别失败时保留具体原因，同时仍可手动选择食物，不影响喂食功能。
- 项目默认密钥支持 `QWEN_API_KEY` 或百炼常用的 `DASHSCOPE_API_KEY`；本机管理页保存的 Key 仍使用 Android Keystore 加密。

## 版本信息

- 版本：2.8.5
- versionCode：23
- 数据库结构不变，可直接覆盖 2.8.4 安装并保留数据。
